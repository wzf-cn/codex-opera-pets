# 贡献角色

1. 在 `pets/<play-id>/<role-id>/` 新增角色，原创角色放在 `pets/original/`。ID 使用小写英文、数字和连字符，并确保安装 ID 全库唯一。
2. 提供 `pet.json`、`spritesheet.webp`、`preview.gif`、角色 README 和 `qa/validation-summary.json`；图集使用 v2，包含九种标准状态及 16 个视线方向。
3. README 记录中文名、所属剧目、行当、造型参考、动作设计、生成或绘制方式、作者和素材许可。未知项留空或标注待核查。
4. 为传统剧目角色说明参考版本；不要把原创设计标为剧目原有角色，也不要声称尚未完成的收录。
5. 提供动作与方向预览，检查裁切、透明背景、角色一致性和循环衔接，并记录 SHA-256。区分文件检查与应用实际播放验证。
6. 更新根目录 `catalog.json` 和 README 角色表；提交中只包含公开素材，不含本机路径、密钥、个人会话或未授权参考图。
7. 提交者应有权按 CC BY 4.0 发布所贡献内容；如含第三方素材，逐项注明来源及许可，供维护者审核。

优先通过 Pull Request 提交；也可用 Issue 建议剧目与角色。

对外统一称“卡通人偶”；文档、简介、预览说明和下载标签遵循此称呼。`pet.json`、`pets/` 和现有机器字段仅为兼容格式，保持不变。
